<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme — kept lightweight as Compose handles theming. -->
    <style name="Theme.MealMate" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:colorBackground">@color/minimalist_background</item>
        <item name="android:statusBarColor">@color/minimalist_background</item>
        <item name="android:navigationBarColor">@color/minimalist_background</item>
    </style>
</resources>